[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure unit tests — no network, no external provider
    integration: mocked-provider integration tests — no real network
    real_provider: real external provider tests — opt-in, requires VOXDESK_REAL_INTEGRATION=1
