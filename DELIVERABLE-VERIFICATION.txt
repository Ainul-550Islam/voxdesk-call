VoxDesk — deliverable verification record
Generated by /home/user/deliverables/verify_batches.py  (re-runnable: python3 /home/user/deliverables/verify_batches.py)

Method
------
For every "===== FILE: <path> (N lines, sha256 <digest>) =====" block in each
STEP17_BATCH0*_DELIVERABLE.md:
  1. the block body is re-extracted from the markdown,
  2. its sha256 is recomputed,
  3. the file's bytes on disk are re-read and hashed,
  4. the block is declared identical only when all three agree.

Result
------
STEP17_BATCH01_DELIVERABLE.md: 0 blocks parsed (older, pre-sha256 header format;
                               verified separately below)
STEP17_BATCH02_DELIVERABLE.md: 6 blocks, 6 byte-identical, 0 mismatched
STEP17_BATCH03_DELIVERABLE.md: 25 blocks, 25 byte-identical, 0 mismatched
STEP17_BATCH04A_DELIVERABLE.md: 110 blocks, 110 byte-identical, 0 mismatched
STEP17_BATCH04B_DELIVERABLE.md: 80 blocks, 80 byte-identical, 0 mismatched
STEP17_BATCH05_DELIVERABLE.md: 9 blocks, 9 byte-identical, 0 mismatched

TOTAL: 230 file blocks verified; identical=230; mismatched=0

Batch 01 (independent check, same method, no sha in that file's headers)
----------------------------------------------------------------------
19 of 20 files byte-identical to the working tree.
1 file differs: tests/test_enterprise_batch01.py
  deliverable 928 lines vs working tree 936 lines; the working tree revision is
  newer (5 code differences: ToolConfig import dropped; two tests replaced by
  test_id_stable_across_content_edits; PUBLISHED->PUBLISHED and
  RETIRED->PUBLISHED transition assertions; test_start_and_project added;
  controlled-action loop over eval/exec/import/os.system; helper reordering;
  duplicated line removed; test renamed). The working tree is the source of
  truth for that file; it is not part of any batch below.

Repository sync verification
----------------------------
After copying the nested tree's newer files into the root tree:
  nested files checked: 1001
  mismatched/missing in root: 0
  (a later re-run of the same check showed 2 files differing — app/main.py and
  app/services/inbox_service.py — because this session edited those two in the
  root tree afterwards; the root copies are the newer ones.)

Test verification (this working tree)
-------------------------------------
tests/test_enterprise_persistence.py                             15 passed
tests/test_enterprise_batch02.py                                 43 passed
tests/test_enterprise_batch01.py                                 80 passed
test_api_contract + test_deployment + test_config              128 passed
full suite (python3 -m pytest tests -q)  2505 passed, 43 skipped
                                         (two runs: 159.48 s and 150.15 s)

Batch 05 (durable storage for automation / notification / inbox)
----------------------------------------------------------------
Schema: alembic chain is now single-linear
  0001_baseline -> ... -> 0011_side_effect_exactly_once -> 0012_enterprise_persistence
  (one head; tests/test_deployment.py asserts the new head).
Tables added: automations, automation_runs, notification_templates,
  notifications, inbox_thread_states (all tenant-scoped, cascading from tenants;
  the inbox overlay also cascades from calls).
Restart proof: tests/test_enterprise_persistence.py wipes every in-process
  registry between requests (a fresh worker's starting state) and re-reads
  through the API, so a passing assertion means the data came out of the
  database. Covered: automation definitions + run history + still-enabled
  state; template/notification round-trip with the recipient still masked;
  dedupe key and read flag; inbox priority/assignee/tags/notes/unread/SLA;
  escalation + reopen still following the transition table; refused writes
  leaving nothing behind; cross-tenant hydration isolation.

Referenced-path audit
---------------------
Every build context and referenced path in docker-compose*.yml, Caddyfile,
Dockerfile, Makefile and .github/workflows/*.yml exists, except
"Dockerfile: dashboard/dist" which is a build output produced by the dashboard
build stage (expected to be absent in a source checkout).

End-to-end patch verification (fresh clone)
-------------------------------------------
  git clone https://github.com/Ainul-550Islam/voxdesk.git /tmp/verify_clone
  git apply --check voxdesk-code-sync.patch   -> clean
  git apply voxdesk-code-sync.patch           -> applied (300 files, 49837 insertions, 11 deletions)
  python3 -m pytest tests/test_enterprise_batch02.py -q
                                              -> 43 passed in 7.74s
  app/realtime/, app/api/{automation,notification,inbox}_routes.py present

Second patch (Batch 05), applied on top of the synced tree (/tmp/verify2)
------------------------------------------------------------------------
  git apply --check batch05-persistence.patch -> clean
  python3 -m pytest tests/test_enterprise_persistence.py -q        -> 15 passed
  python3 -m pytest tests/test_enterprise_batch02.py tests/test_deployment.py -q
                                                                   -> 75 passed

Git-history scrub (PII dumps) — rehearsal, not yet applied to this repo
-----------------------------------------------------------------------
  mirror clone -> git filter-repo --invert-paths --path backups/ --force
  commits before/after: 7 / 7 (history preserved)
  paths under backups/ in any commit: 4 before -> 0 after
  HEAD tree: identical to the original minus backups/ (15111 files, 0 under backups/)
  The working repo still contains the dumps in one commit (bfb0cef); the rewrite
  has not been run here and nothing has been force-pushed. See
  /home/user/deliverables/HISTORY-SCRUB.md and git-history-scrub.sh.
